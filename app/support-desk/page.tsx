export default function SupportDeskHomePage() {
  return (
    <section className="hidden min-h-[calc(100vh-3.5rem)] items-center justify-center p-8 lg:flex">
      <div className="max-w-md text-center">
        <h1 className="text-xl font-semibold text-ink">Support Service</h1>
        <p className="mt-2 text-sm text-muted">
          Pick a store on the left. Name, Store ID, contact, the 1-hour timer, and the full chat stay on this page so the
          team can reply without leaving the desk.
        </p>
      </div>
    </section>
  );
}
