Catalog photographs used by Distribution Center.

- Each `{sku}.jpg` is a realistic product still matched to that SKU’s title (Unsplash, Wikimedia Commons, Openverse, or DummyJSON product photos).
- Files are saved locally as 900×900 JPEG so the shop does not depend on a third-party CDN at runtime.

Replace any file in this folder with a licensed photo of the same name (`{sku}.jpg`) without a database change.
